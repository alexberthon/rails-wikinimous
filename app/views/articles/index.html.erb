<h1>Articles</h1>

<ul class="list-unstyled">
  <% @articles.each do |article| %>
    <li>
      <h3>
        <%= link_to article.title, article_path(article) %>
     </h3>
      <p><%= article.content[0, 140] %></p>
    </li>
  <% end %>
</ul>
